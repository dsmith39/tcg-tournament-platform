# Copy this file to .env and fill in real values before running the server.
PORT=5000
HOST=127.0.0.1
JWT_SECRET=replace-with-a-strong-random-secret-at-least-32-chars

# No database URL needed -- storage is SQLite (server/data/app.db and
# card-database/data/cards.db), created automatically on first run.
